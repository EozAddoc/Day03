# **Exercice 7 : Tracer un graphique simple**
#   
#   - Créez un vecteur `x` contenant les valeurs `1, 2, 3, 4, 5`.
# - Créez un vecteur `y` contenant les valeurs `10, 20, 30, 40, 50`.
# - Tracez un graphique linéaire avec la fonction `plot(x, y)`.

x = c(1,2,3,4,5)
y=c(10,20,30,40,50)
plot(x,y)

