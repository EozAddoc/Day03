# **Exercice 13 : Boucle `for`**
#   
#   - Créez un vecteur `nombres` contenant les valeurs `1, 2, 3, 4, 5`.
# - Utilisez une boucle `for` pour afficher le double de chaque nombre.

nombres = c(1, 2, 3, 4, 5)

for (x in nombres){
  print(x*2)
}

