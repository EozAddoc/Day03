# **Exercice 9 : Utilisation d’une fonction**
#   
#   - Créez une fonction `carre()` qui prend un nombre en paramètre et retourne son carré.
# - Testez la fonction avec les valeurs `4` et `7`.

carre <- function(x) {
  return(x*x)
}

print(carre(4))
print(carre(7))
