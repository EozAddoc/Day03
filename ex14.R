# **Exercice 14 : Conditions avec `if`**
#   
#   - Créez une variable `note` contenant la valeur `16`.
# - Écrivez une condition `if` pour afficher `"Réussi"` si la note est supérieure ou égale à `10`, sinon affichez `"Échoué"`.

note = 16

if(note>10){
  print("Réussi")
}else{
  print("Échoué")
}